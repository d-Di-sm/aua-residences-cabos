/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useMemo } from "react";
import { useGLTF, useTexture } from "@react-three/drei";
import * as THREE from "three";

const Cactus01 = (props) => {
  const { nodes, materials } = useGLTF("/models/Model02/Cactus01.glb");

  const cactusTexture = useTexture("./textures/vegetation/Cactus01_.jpg");
  cactusTexture.flipY = false;
  cactusTexture.colorSpace = THREE.SRGBColorSpace;

  // Acero
  const cactusApt = useMemo(() => {
    return new THREE.MeshStandardMaterial({
      map: cactusTexture,
      toneMapped: false, // prevent threejs from washing colors
    });
  }, [cactusTexture]);

  return (
    <group {...props} dispose={null}>
      <mesh
        castShadow
        receiveShadow
        geometry={nodes.temp_meshply925.geometry}
        // material={materials["Material_0.012"]}
        material={cactusApt}
        position={[124.358, 23.024, -41.275]}
        rotation={[Math.PI, -0.859, Math.PI]}
        scale={1.132}
      />
    </group>
  );
};

useGLTF.preload("/models/Model02/Cactus01.glb");

export default Cactus01;
